<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.pumluda.infrastructure.dao.IMarketingUserDao">

    <resultMap id="dataMap" type="MarketingUserPo">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password_hash" property="passwordHash"/>
        <result column="role" property="role"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="findByUsername" resultMap="dataMap">
        SELECT id, username, password_hash, role, create_time, update_time
        FROM group_buy_market_better.marketing_user
        WHERE username = #{username}
    </select>

    <insert id="insert" parameterType="MarketingUserPo">
        INSERT INTO group_buy_market_better.marketing_user (username, password_hash, role, create_time, update_time)
        VALUES (#{username}, #{passwordHash}, #{role}, NOW(), NOW())
    </insert>

    <update id="updatePassword">
        UPDATE group_buy_market_better.marketing_user
        SET password_hash = #{passwordHash}, update_time = NOW()
        WHERE username = #{username}
    </update>

</mapper>
